// The tick (in miliseconds) in which humanoids are replicated
export const HUMANOID_UPDATE_TICK = 10;

// The tick (in miliseconds) in which zombies are simulated on the server
export const ZOMBIE_UPDATE_TICK = 10;

export const SHOW_DEBUG_LOG = true;
//export const SHOW_DEBUG_LOG = false;